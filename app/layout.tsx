import "./globals.css";

export const metadata = {
  title: "Coffey Casino",
  description: "A tiny slot machine game from Coffey Casino.",
  icons: {
    icon: "/favicon.svg",
    shortcut: "/favicon.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
